# Salesforce Connected App 設定
#
# セットアップ手順:
# 1. このファイルを .env にコピー
# 2. Salesforce で Connected App を作成
# 3. 以下の値を設定

# Salesforce Connected App の Consumer Key
VITE_SALESFORCE_CLIENT_ID=your_consumer_key_here

# OAuth コールバック URL（本番環境）
# GitHub Pages: https://s-hoshioka.github.io/salesforce-dashboard/callback
# ローカル開発: http://localhost:5175/callback
VITE_SALESFORCE_REDIRECT_URI=http://localhost:5175/callback

# 注意:
# - これらの環境変数が設定されていない場合、デモモード（モックデータ）で動作します
# - .env ファイルは .gitignore に含まれているため、Git にコミットされません
